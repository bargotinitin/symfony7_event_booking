# My Custom Package Recipe

This recipe configures the `my_custom_package` bundle with a simple parameter.

## Installation

To install the package, use the following command:

```bash
composer require my_vendor/my_custom_package
